import React from 'react';

import '../styles/canvas.scss';

const Canvas = () => {
  return (
    <div class="canvas">
      <canvas width={600} height={400} />
    </div>
  );
};

export default Canvas;